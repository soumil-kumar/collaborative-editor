{
  "name": "collaborative-code-editor",
  "version": "1.0.0",
  "private": true,
  "description": "Real-Time Collaborative Code Execution Platform",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "node server/index.js"
  }
}

